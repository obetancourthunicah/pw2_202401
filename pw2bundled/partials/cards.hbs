<section class="todoCards">
    {{#each todos}}
    <div class="card">
        <img src="{{imgUrl}}" alt="{{title}}" />
        <h3>{{title}}</h3>
        <p>{{content}}</p>
    </div>
    {{/each}}
</section>